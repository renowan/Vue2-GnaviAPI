
export const MIN_LOADING_ANIMATION_MS = 600
export const DEFAULT_AJAX_TIMEOUT_MS = 10000
export const DOMAIN = !window.process ? '' : 'http://api.gnavi.co.jp'
export const KEY_ID = ''
export const IS_USE_STATIC_JSON = false
export const params = {
  keyid: KEY_ID,
  format: 'json'
}
